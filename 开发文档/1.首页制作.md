1. 完成首页导航条制作 ： 配置顶部安全距离 然后给导航条添加安全距离的padding-top

2. 完成轮播图的全局组件
    完成静态结构搭建
    完成全局自动导入组件
    完成轮播图api请求，并在首页请求然后数据传递给轮播图组件

3. 首页分类制作
    静态搭建 page设置为f7f7f7
    渲染数据 配置对应数据类型接口

4. 热门推荐
    静态搭建 
    渲染数据 配置对应数据类型接口

5. 猜你喜欢
  搭建全局组件
  渲染数据 配置对应数据类型接口
  下滑自动加载更多数据 注意要给scoll-view一个高度才能触发底部事件(100vh)
    页面划到底部触发事件在这个事件里面请求子组件的方法去获取更多数据
    在子组件将方法给父组件
    子组件配置好 首先push到原来的数组 然后请求参数必须都++
    优化：当没有更多页码的时候提示已经加载到最后一页了然后不再请求

6. 下拉刷新
  使用 @refresherrefresh 事件触发下拉刷新
  使用 refresher-enabled 开启下拉刷新
  使用 refresher-triggered 控制下拉刷新状态
  guess组件需要重置数据然后再获取新的数据
  使用Promise.all 并行请求多个接口
    
